import { ExcelDirective } from './excel.directive';

describe('ExcelDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcelDirective();
    expect(directive).toBeTruthy();
  });
});
